<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Create a form to show/edit users -->
  <fieldset>
  <%= f.label :first_name %>
  <%= f.text_field :fname %>
  <br>
  <%= f.label :last_name %>
  <%= f.text_field :lname %>
  <br>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <br>
  <%= f.label :password %>
  <%= f.text_field :password %>
  <br>
  <!-- Need to add password confirmation setup here -->

  </fieldset>


  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>

  